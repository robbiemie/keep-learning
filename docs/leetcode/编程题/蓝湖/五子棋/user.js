class User {
    constructor(props) {
        this.id = props.id
        this.type = props.type
        this.name = props.name
    }
    click(e) {
        // 下棋
    }
    restore() {
        // 悔棋
    }
}